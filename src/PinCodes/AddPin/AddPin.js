import React from 'react'

export default function AddPin() {
    return (
        <div>

            <h1>Add DATA::</h1>
            {/* <input onChange={handleData} name="pinCode" type="text"></input>
                <button onClick={() => props.afterClick(inputValue)}>FINAL ADD</button> */}

        </div>
    )
}
